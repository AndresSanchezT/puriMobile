<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="14dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="#E8D5B7"
    app:cardBackgroundColor="#FFFFFF">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Nombre del producto (fila 1, ancho máximo) -->
        <TextView
            android:id="@+id/tvNombreProducto"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Producto"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#2C1810"
            android:ellipsize="end"
            android:maxLines="1"
            android:layout_marginBottom="8dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Queso Mantecoso 1kg" />

        <!-- Fila 2: Cantidad y Precio Unitario -->
        <LinearLayout
            android:id="@+id/layoutCantidadPrecio"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="start|center_vertical"
            android:layout_marginBottom="8dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvNombreProducto">

            <!-- Cantidad -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:cardCornerRadius="8dp"
                app:cardElevation="0dp"
                app:strokeWidth="0dp"
                app:cardBackgroundColor="#F5E6D3">

                <TextView
                    android:id="@+id/tvCantidad"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="x1"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="#6B4226"
                    android:paddingHorizontal="10dp"
                    android:paddingVertical="4dp"
                    tools:text="x5" />

            </com.google.android.material.card.MaterialCardView>

            <!-- Separador -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="·"
                android:textSize="16sp"
                android:textColor="#E8D5B7"
                android:layout_marginStart="6dp"
                android:layout_marginEnd="6dp" />

            <!-- Precio Unitario -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:cardCornerRadius="8dp"
                app:cardElevation="0dp"
                app:strokeWidth="1dp"
                app:strokeColor="#E8854D"
                app:cardBackgroundColor="#FDECD5">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="4dp">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="S/."
                        android:textSize="11sp"
                        android:textStyle="bold"
                        android:textColor="#E8854D" />

                    <TextView
                        android:id="@+id/tvPrecioUnitario"
                        android:layout_width="80dp"
                        android:layout_height="wrap_content"
                        android:text="0.00"
                        android:textSize="14sp"
                        android:textStyle="bold"
                        android:textColor="#E8854D"
                        android:gravity="end"
                        tools:text="10550.00" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text=" c/u"
                        android:textSize="10sp"
                        android:textColor="#A67B5B"
                        android:layout_marginStart="2dp" />

                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

        </LinearLayout>

        <!-- Fila 3: Importe + Botón Eliminar -->
        <LinearLayout
            android:id="@+id/layoutImporteEliminar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end|center_vertical"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/layoutCantidadPrecio">

            <!-- Importe -->
            <TextView
                android:id="@+id/tvPrecioItem"
                android:layout_width="120dp"
                android:layout_height="wrap_content"
                android:text="S/. 0.00"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#E8854D"
                android:gravity="end"
                android:ellipsize="end"
                android:maxLines="1"
                tools:text="10550.00"
                android:layout_marginEnd="8dp" />

            <!-- Botón Eliminar -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/btnEliminarCard"
                android:layout_width="40dp"
                android:layout_height="40dp"
                app:cardCornerRadius="10dp"
                app:cardElevation="0dp"
                app:strokeWidth="0dp"
                app:cardBackgroundColor="#FFCDD2">

                <ImageView
                    android:id="@+id/btnEliminar"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:src="@drawable/ic_delete"
                    android:padding="10dp"
                    app:tint="#E53935"
                    android:contentDescription="Eliminar producto"
                    android:clickable="true"
                    android:focusable="true"
                    android:background="?attr/selectableItemBackgroundBorderless" />

            </com.google.android.material.card.MaterialCardView>

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
